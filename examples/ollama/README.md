# Ollama status

Direct Ollama tool-loop support is scheduled for Version 0.6. This directory
documents that boundary so users are not given a configuration that implies an
unimplemented or untested adapter.

The future adapter will generate tool declarations from the shared MCP schemas and
call the same core services.
