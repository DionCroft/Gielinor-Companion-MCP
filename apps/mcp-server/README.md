# @gielinor/mcp-server

The local stdio transport for Gielinor Companion MCP. Build it from the repository root
with `pnpm build`, then start it with `pnpm start:mcp`.

Only JSON-RPC is written to stdout. Diagnostics use stderr.
